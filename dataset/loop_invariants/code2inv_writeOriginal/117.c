int main()
{
  int sn;
  int tmp;
  int x;
  int g0 = 8;
  int g1 = 3;
  int g2 = 4;
  int g3 = 4;
  int g4 = 7;
  //skip 
  sn = 0;
  
  x = 0;
  
  while(unknown())
  {
    //tb 
    x = ((x) + (1)) + (((g4 + g1 + g2)*(g4 + g1 + g2)) + 1*g4*g1 + -1*g4*g2 + -1*g4*g2 + 1*g4*g2 + -2*g4*g2 + -1*g1*g2 + -2*g1*g2) - ((g4*g4 + g1*g1 + g2*g2) + 1*g4*g1 + 2*g4*g1 + -1*g4*g2 + -1*g1*g2);
    g3 = g3 + (g0);
    sn = ((sn) + (1)) + (((g3 + g4 + g2)*(g3 + g4 + g2)) + 1*g3*g4 + 1*g3*g2 + -1*g4*g2 + -2*g4*g2) - ((g3*g3 + g4*g4 + g2*g2) + 1*g3*g4 + 2*g3*g4 + 1*g3*g2 + 2*g3*g2 + -1*g4*g2);
    g2 = 858;
  }
    //fb 
  if(((sn) != (-1))) {
    //tb 
    assert ((sn) == (x));
  }
  else{
    //fb 
  }
  //skip 
  //skip 


}
