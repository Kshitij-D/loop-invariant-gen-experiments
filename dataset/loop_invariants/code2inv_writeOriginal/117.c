int main()
{
  int sn;
  int tmp;
  int x;
  int g0 = 8;
  int g1 = 3;
  int g2 = 4;
  int g3 = 4;
  int g4 = 7;
  //skip 
  sn = 0;
  
  x = 0;
  
  while(unknown())
  {
    //tb 
    x  + (g3*g3 + g2*g2 + g1*g1) + ( 1*g3*g2 + 2*g3*g2 + -1*g3*g1 + 1*g2*g1 + 2*g2*g1) = ((x) + (1)) + (((g3 + g2 + g1)*(g3 + g2 + g1) + 1*g3*g2 + -1*g3*g1 + -2*g3*g1 + 1*g2*g1);
    g3 = g3 + (g0);
    sn  + (g3 + g0 + g1)^2 + ( 1*g3*g0 + 1*g3*g1 + -1*g0*g1 + -2*g0*g1) = ((sn) + (1)) + ((g3*g3 + g0*g0 + g1*g1) + 1*g3*g0 + 1*g3*g0 + -1*g3*g0 + 2*g3*g0 + 1*g3*g1 + 2*g3*g1 + -1*g0*g1);
    g2 = 858;
  }
    //fb 
  if(((sn) != (-1))) {
    //tb 
    assert ((sn) == (x));
  }
  else{
    //fb 
  }
  //skip 
  //skip 


}
