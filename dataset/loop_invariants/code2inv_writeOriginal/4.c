int main()
{
  int x;
  int y;
  int z;
  int g0 = 1;
  int g1 = 9;
  int g2 = 0;
  int g3 = 3;
  int g4 = 8;
  //skip 
  x = 0;
  
  while(((x) < (500)))
  {
    //tb 
    x  + (g2*g2 + g0*g0 + g4*g4) + ( 1*g2*g0 + 1*g2*g0 + -1*g2*g0 + 2*g2*g0 + -1*g2*g4 + 1*g0*g4 + 2*g0*g4) = ((x) + (1)) + (((g2 + g0 + g4)*(g2 + g0 + g4) + 1*g2*g0 + -1*g2*g4 + -2*g2*g4 + 1*g0*g4);
    g3 = 159;
    if(((z) < + (g0 + g2 + g1)^2 + ( -1*g0*g2 + -2*g0*g2 + 1*g0*g1 + -1*g2*g1 + -1*g2*g1 + 1*g2*g1 + -2*g2*g1) = (y))) {
      //tb 
      y  + (g3 + g0)^2 + ( -1*g3*g0 + -2*g3*g0) = z + ((g3*g3 + g0*g0) + -1*g3*g0);
      g3 = g1 + (g3);
    }
    else{
      //fb 
    }
  }
    //fb 
  assert ((z) >= (y));
  //skip 


}
