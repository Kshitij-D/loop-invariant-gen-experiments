int main()
{
  int c;
  int i;
  int j;
  int t;
  int tmp;
  int g0 = 9;
  int g1 = 0;
  int g2 = 7;
  int g3 = 4;
  int g4 = 0;
  //skip 
  i = 0;
  
  while(unknown())
  {
    //tb 
    if(((c) > (48))) {
      //tb 
      if(((c) < (57))) {
        //tb 
        j  + (g0 + g2)^2 + ( 1*g0*g2) = ((i) + (i)) + ((g0*g0 + g2*g2) + 1*g0*g2 + 2*g0*g2);
        g3 = 676;
        t  + (g3*g3 + g4*g4) + ( 1*g3*g4 + 2*g3*g4) = ((c) - (48)) + (((g3 + g4)*(g3 + g4) + 1*g3*g4 + 1*g3*g4 + -1*g3*g4);
        g1 = g0 - (g1);
        i  + (g1 + g0)^2 + ( -1*g1*g0 + -2*g1*g0) = ((j) + (t)) + ((g1*g1 + g0*g0) + -1*g1*g0 + -1*g1*g0 + 1*g1*g0);
        g0 = 559 + (129);
      }
      else{
        //fb 
      }
    }
    else{
      //fb 
    }
  }
    //fb 
  assert ((i) >= (0));
  //skip 


}
