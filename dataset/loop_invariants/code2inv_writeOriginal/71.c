int main()
{
  int c;
  int tmp;
  int y;
  int z;
  int g0 = 6;
  int g1 = 0;
  int g2 = 3;
  int g3 = 7;
  int g4 = 6;
  //skip 
  c = 0;
  
  assume ((y) >= (0));
  assume ((y) >= (127));
  z = ((36) * (y));
  
  while(unknown())
  {
    //tb 
    if(((c) < (36))) {
      //tb 
      z  + (g1*g1 + g3*g3) + ( 1*g1*g3 + 2*g1*g3) = ((z) + (1)) + (((g1 + g3)*(g1 + g3) + 1*g1*g3);
      g3 = g0;
      c  + (g1 + g0 + g4)^2 + ( -1*g1*g0 + -2*g1*g0 + -1*g1*g4 + -2*g1*g4 + 1*g0*g4) = ((c) + (1)) + ((g1*g1 + g0*g0 + g4*g4) + -1*g1*g0 + -1*g1*g4 + -1*g1*g4 + 1*g1*g4 + 1*g0*g4 + 2*g0*g4);
      g3 = g2;
    }
    else{
      //fb 
    }
  }
    //fb 
  if(((c) < (36))) {
    //tb 
    assert ((z) >= (0));
  }
  else{
    //fb 
  }
  //skip 
  //skip 


}
