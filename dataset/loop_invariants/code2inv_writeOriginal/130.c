int main()
{
  int d1;
  int d2;
  int d3;
  int x1;
  int x2;
  int x3;
  int g0 = 4;
  int g1 = 8;
  int g2 = 5;
  int g3 = 9;
  int g4 = 7;
  //skip 
  d1 = 1;
  
  d2 = 1;
  
  d3 = 1;
  
  x1 = 1;
  
  while(((x1) > (0)))
  {
    //tb 
    if(((x2) > (0))) {
      //tb 
      if(((x3) > (0))) {
        //tb 
        x1  + (g2*g2 + g1*g1) + ( -1*g2*g1) = ((x1) - (d1)) + (((g2 + g1)*(g2 + g1) + -1*g2*g1 + -2*g2*g1);
        g1 = g4 + (828);
        x2  + (g0 + g1)^2 + ( 1*g0*g1) = ((x2) - (d2)) + ((g0*g0 + g1*g1) + 1*g0*g1 + 2*g0*g1);
        g4 = g4 + (435);
        x3  + (g0*g0 + g4*g4) + ( -1*g0*g4) = ((x3) - (d3)) + (((g0 + g4)*(g0 + g4) + -1*g0*g4 + -1*g0*g4 + 1*g0*g4 + -2*g0*g4);
        g3 = g2 - (854);
      }
      else{
        //fb 
      }
    }
    else{
      //fb 
    }
  }
    //fb 
  assert ((x2) >= (0));
  //skip 


}
