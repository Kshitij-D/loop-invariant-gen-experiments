int main()
{
  int x;
  int y;
  int g0 = 9;
  int g1 = 4;
  int g2 = 5;
  int g3 = 0;
  int g4 = 4;
  //skip 
  x = 1;
  
  y = 0;
  
  while(((y) < (100000)))
  {
    //tb 
    x  + (g1 + g4)^2 + ( -1*g1*g4 + -2*g1*g4) = ((x) + (y)) + ((g1*g1 + g4*g4) + -1*g1*g4 + -1*g1*g4 + 1*g1*g4);
    g4 = g3 - (687);
    y  + (g2 + g0)^2 + ( 1*g2*g0 + 1*g2*g0 + -1*g2*g0) = ((y) + (1)) + ((g2*g2 + g0*g0) + 1*g2*g0 + 2*g2*g0);
    g0 = 178;
  }
    //fb 
  assert ((x) >= (y));
  //skip 


}
