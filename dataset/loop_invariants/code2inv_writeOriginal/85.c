int main()
{
  int x;
  int y;
  int g0 = 9;
  int g1 = 0;
  int g2 = 7;
  int g3 = 0;
  int g4 = 3;
  //skip 
  x = -15000;
  
  while(((x) < (0)))
  {
    //tb 
    x  + (g0 + g2)^2 + ( -1*g0*g2 + -2*g0*g2) = ((x) + (y)) + ((g0*g0 + g2*g2) + -1*g0*g2);
    g1 = 286 + (g3);
    y  + (g3*g3 + g0*g0) + ( -1*g3*g0) = ((y) + (1)) + (((g3 + g0)*(g3 + g0) + -1*g3*g0 + -2*g3*g0);
    g2 = g0 + (g0);
  }
    //fb 
  assert ((y) > (0));
  //skip 


}
