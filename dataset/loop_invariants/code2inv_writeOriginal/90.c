int main()
{
  int lock;
  int tmp;
  int x;
  int y;
  int g0 = 9;
  int g1 = 3;
  int g2 = 6;
  int g3 = 6;
  int g4 = 9;
  //skip 
  y = ((x) + (1));
  
  lock = 0;
  
  while(((x) != (y)))
  {
    //tb 
    if(unknown()) {
      //tb 
      lock = 1 + (((g0 + g3 + g2)*(g0 + g3 + g2)) + 1*g0*g3 + 1*g0*g2 + 1*g3*g2) - ((g0*g0 + g3*g3 + g2*g2) + 1*g0*g3 + 2*g0*g3 + 1*g0*g2 + 1*g0*g2 + -1*g0*g2 + 2*g0*g2 + 1*g3*g2 + 2*g3*g2);
      g4 = g1 + (g0);
      x = y + ((g4*g4 + g3*g3) + -1*g4*g3) - (((g4 + g3)*(g4 + g3)) + -1*g4*g3 + -2*g4*g3);
      g1 = g2 - (g4);
    }
    else{
      //fb 
      lock = 0;
      g0 = 310 + (561);
      x = y;
      g0 = 184;
      y = ((y) + (1));
      g4 = g4 + (143);
    }
  }
    //fb 
  assert ((lock) == (1));
  //skip 


}
