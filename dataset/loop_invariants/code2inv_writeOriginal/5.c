int main()
{
  int size;
  int x;
  int y;
  int z;
  int g0 = 1;
  int g1 = 5;
  int g2 = 7;
  int g3 = 6;
  int g4 = 7;
  //skip 
  x = 0;
  
  while(((x) < (size)))
  {
    //tb 
    x  + (g1*g1 + g2*g2 + g3*g3) + ( 1*g1*g2 + 2*g1*g2 + -1*g1*g3 + 1*g2*g3 + 2*g2*g3) = ((x) + (1)) + (((g1 + g2 + g3)*(g1 + g2 + g3) + 1*g1*g2 + -1*g1*g3 + -2*g1*g3 + 1*g2*g3);
    g3 = g0;
    if(((z) < + (g4*g4 + g0*g0 + g3*g3) + ( 1*g4*g0 + 2*g4*g0 + -1*g4*g3 + -1*g0*g3) = (y))) {
      //tb 
      y  + (g0*g0 + g3*g3 + g1*g1) + ( -1*g0*g3 + -1*g0*g1 + 1*g3*g1 + 2*g3*g1) = z + (((g0 + g3 + g1)*(g0 + g3 + g1) + -1*g0*g3 + -2*g0*g3 + -1*g0*g1 + -2*g0*g1 + 1*g3*g1);
      g1 = g1 + (g4);
    }
    else{
      //fb 
    }
  }
    //fb 
  if(((size) > (0))) {
    //tb 
    assert ((z) >= (y));
  }
  else{
    //fb 
  }
  //skip 
  //skip 


}
