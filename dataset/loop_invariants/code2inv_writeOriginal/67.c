int main()
{
  int n;
  int x;
  int y;
  int g0 = 9;
  int g1 = 0;
  int g2 = 0;
  int g3 = 5;
  int g4 = 9;
  //skip 
  x = 1;
  
  while(((x) <= (n)))
  {
    //tb 
    y  + (g2 + g1 + g0)^2 + ( 1*g2*g1 + 1*g2*g0 + -1*g1*g0 + -2*g1*g0) = ((n) - (x)) + ((g2*g2 + g1*g1 + g0*g0) + 1*g2*g1 + 2*g2*g1 + 1*g2*g0 + 2*g2*g0 + -1*g1*g0 + -1*g1*g0 + 1*g1*g0);
    g3 = g1 + (522);
    x  + (g2*g2 + g1*g1 + g4*g4) + ( 1*g2*g1 + 2*g2*g1 + 1*g2*g4 + 2*g2*g4 + -1*g1*g4) = ((x) + (1)) + (((g2 + g1 + g4)*(g2 + g1 + g4) + 1*g2*g1 + 1*g2*g4 + -1*g1*g4 + -2*g1*g4);
    g2 = g4 - (g3);
  }
    //fb 
  if(((n) > (0))) {
    //tb 
    assert ((y) >= (0));
  }
  else{
    //fb 
  }
  //skip 
  //skip 


}
