int main()
{
  int x;
  int y;
  int z;
  int g0 = 5;
  int g1 = 8;
  int g2 = 5;
  int g3 = 3;
  int g4 = 9;
  //skip 
  x = 0;
  
  while(((x) < (5)))
  {
    //tb 
    x  + (g4*g4 + g2*g2 + g1*g1) + ( -1*g4*g2 + -1*g4*g1 + -1*g2*g1) = ((x) + (1)) + (((g4 + g2 + g1)*(g4 + g2 + g1) + -1*g4*g2 + -2*g4*g2 + -1*g4*g1 + -2*g4*g1 + -1*g2*g1 + -2*g2*g1);
    g3 = 399 - (g2);
    if(((z) < + (g1*g1 + g4*g4 + g0*g0) + ( -1*g1*g4 + -1*g1*g0 + -1*g1*g0 + 1*g1*g0 + -1*g4*g0) = (y))) {
      //tb 
      y  + (g2 + g4 + g0)^2 + ( -1*g2*g4 + -2*g2*g4 + -1*g2*g0 + -1*g2*g0 + 1*g2*g0 + -2*g2*g0 + 1*g4*g0) = z + ((g2*g2 + g4*g4 + g0*g0) + -1*g2*g4 + -1*g2*g0 + 1*g4*g0 + 2*g4*g0);
      g2 = g0 + (g1);
    }
    else{
      //fb 
    }
  }
    //fb 
  assert ((z) >= (y));
  //skip 


}
