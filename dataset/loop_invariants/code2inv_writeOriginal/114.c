int main()
{
  int sn;
  int tmp;
  int x;
  int g0 = 4;
  int g1 = 5;
  int g2 = 8;
  int g3 = 1;
  int g4 = 3;
  //skip 
  sn = 0;
  
  x = 0;
  
  while(unknown())
  {
    //tb 
    x  + (g3 + g4 + g2)^2 + ( 1*g3*g4 + 1*g3*g2 + 1*g4*g2) = ((x) + (1)) + ((g3*g3 + g4*g4 + g2*g2) + 1*g3*g4 + 2*g3*g4 + 1*g3*g2 + 2*g3*g2 + 1*g4*g2 + 2*g4*g2);
    g1 = 352;
    sn  + (g3*g3 + g1*g1 + g2*g2) + ( 1*g3*g1 + 2*g3*g1 + -1*g3*g2 + -1*g3*g2 + 1*g3*g2 + -1*g1*g2) = ((sn) + (1)) + (((g3 + g1 + g2)*(g3 + g1 + g2) + 1*g3*g1 + 1*g3*g1 + -1*g3*g1 + -1*g3*g2 + -2*g3*g2 + -1*g1*g2 + -2*g1*g2);
    g2 = g1 - (g2);
  }
    //fb 
  if(((sn) != (x))) {
    //tb 
    assert ((sn) == (-1));
  }
  else{
    //fb 
  }
  //skip 
  //skip 


}
