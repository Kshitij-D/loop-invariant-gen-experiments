int main()
{
  int i;
  int size;
  int sn;
  int g0 = 5;
  int g1 = 2;
  int g2 = 5;
  int g3 = 8;
  int g4 = 9;
  //skip 
  sn = 0;
  
  i = 1;
  
  while(((i) <= (size)))
  {
    //tb 
    i  + (g0*g0 + g4*g4) + ( 1*g0*g4 + 2*g0*g4) = ((i) + (1)) + (((g0 + g4)*(g0 + g4) + 1*g0*g4);
    g2 = 617 + (709);
    sn  + (g0*g0 + g3*g3 + g1*g1) + ( 1*g0*g3 + 2*g0*g3 + 1*g0*g1 + 2*g0*g1 + 1*g3*g1 + 2*g3*g1) = ((sn) + (1)) + (((g0 + g3 + g1)*(g0 + g3 + g1) + 1*g0*g3 + 1*g0*g1 + 1*g3*g1);
    g4 = 168 - (g2);
  }
    //fb 
  if(((sn) != (size))) {
    //tb 
    assert ((sn) == (0));
  }
  else{
    //fb 
  }
  //skip 
  //skip 


}
