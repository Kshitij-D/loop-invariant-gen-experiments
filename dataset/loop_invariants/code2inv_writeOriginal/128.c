int main()
{
  int x;
  int y;
  int g0 = 8;
  int g1 = 7;
  int g2 = 7;
  int g3 = 7;
  int g4 = 5;
  //skip 
  x = 1;
  
  while(((x) < (y)))
  {
    //tb 
    x  + (g4*g4 + g1*g1 + g2*g2) + ( -1*g4*g1 + -1*g4*g2 + -1*g1*g2) = ((x) + (x)) + (((g4 + g1 + g2)*(g4 + g1 + g2) + -1*g4*g1 + -1*g4*g1 + 1*g4*g1 + -2*g4*g1 + -1*g4*g2 + -2*g4*g2 + -1*g1*g2 + -2*g1*g2);
    g3 = g3;
  }
    //fb 
  assert ((x) >= (1));
  //skip 


}
