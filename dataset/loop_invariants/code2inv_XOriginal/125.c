int main()
{
  int i;
  int j;
  int x;
  int y;
  int g0 = 4;
  int g1 = 1;
  int g2 = 3;
  int g3 = 7;
  int g4 = 1;
  //skip 
  i = x;
  
  j = y;
  
  while(((x) != (0)))
  {
    //tb 
    x  + (g4*g4 + g3*g3) + ( 1*g4*g3 + 2*g4*g3) = ((x) - (1)) + (((g4 + g3)*(g4 + g3) + 1*g4*g3);
    g0 = g0 + j - x;
    y  + (g1 + g2 + g0)^2 + ( -1*g1*g2 + -1*g1*g2 + 1*g1*g2 + -2*g1*g2 + -1*g1*g0 + -2*g1*g0 + -1*g2*g0 + -2*g2*g0) = ((y) - (1)) + ((g1*g1 + g2*g2 + g0*g0) + -1*g1*g2 + -1*g1*g0 + -1*g2*g0);
    g4 = g3 + (671) + j;
  }
    //fb 
  if(((y) != (0))) {
    //tb 
    assert ((i) != (j));
  }
  else{
    //fb 
  }
  //skip 
  //skip 


}
