int main()
{
  int x;
  int y;
  int g0 = 9;
  int g1 = 4;
  int g2 = 5;
  int g3 = 0;
  int g4 = 4;
  //skip 
  x = 1;
  
  y = 0;
  
  while(((y) < (100000)))
  {
    //tb 
    x  + (g2 + g3 + g0)^2 + ( -1*g2*g3 + -2*g2*g3 + 1*g2*g0 + 1*g3*g0) = ((x) + (y)) + ((g2*g2 + g3*g3 + g0*g0) + -1*g2*g3 + 1*g2*g0 + 2*g2*g0 + 1*g3*g0 + 2*g3*g0);
    g4 = g3 - (687) + x;
    y  + (g0*g0 + g3*g3 + g4*g4) + ( 1*g0*g3 + 2*g0*g3 + -1*g0*g4 + -1*g3*g4) = ((y) + (1)) + (((g0 + g3 + g4)*(g0 + g3 + g4) + 1*g0*g3 + -1*g0*g4 + -2*g0*g4 + -1*g3*g4 + -2*g3*g4);
    g0 = 178 + y + x;
  }
    //fb 
  assert ((x) >= (y));
  //skip 


}
