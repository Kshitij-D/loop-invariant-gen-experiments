int main()
{
  int i;
  int size;
  int sn;
  int g0 = 5;
  int g1 = 2;
  int g2 = 5;
  int g3 = 8;
  int g4 = 9;
  //skip 
  sn = 0;
  
  i = 1;
  
  while(((i) <= (size)))
  {
    //tb 
    i = ((i) + (1)) + (((g3 + g0 + g4)*(g3 + g0 + g4)) + -1*g3*g0 + -2*g3*g0 + 1*g3*g4 + -1*g0*g4 + -2*g0*g4) - ((g3*g3 + g0*g0 + g4*g4) + -1*g3*g0 + 1*g3*g4 + 2*g3*g4 + -1*g0*g4);
    g2 = 617 + (709) + sn - i;
    sn = ((sn) + (1)) + ((g3*g3 + g4*g4 + g2*g2) + -1*g3*g4 + 1*g3*g2 + 2*g3*g2 + 1*g4*g2 + 2*g4*g2) - (((g3 + g4 + g2)*(g3 + g4 + g2)) + -1*g3*g4 + -2*g3*g4 + 1*g3*g2 + 1*g4*g2);
    g4 = 168 - (g2) + sn;
  }
    //fb 
  if(((sn) != (0))) {
    //tb 
    assert ((sn) == (size));
  }
  else{
    //fb 
  }
  //skip 
  //skip 


}
