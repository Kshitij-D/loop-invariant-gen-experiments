int main()
{
  int x;
  int y;
  int g0 = 7;
  int g1 = 3;
  int g2 = 9;
  int g3 = 9;
  int g4 = 6;
  //skip 
  x = -50;
  
  while(((x) < (0)))
  {
    //tb 
    x  + (g1*g1 + g3*g3) + ( 1*g1*g3 + 2*g1*g3) = ((x) + (y)) + (((g1 + g3)*(g1 + g3) + 1*g1*g3);
    g4 = g4 + x + y;
    y  + (g4 + g2)^2 + ( -1*g4*g2 + -2*g4*g2) = ((y) + (1)) + ((g4*g4 + g2*g2) + -1*g4*g2);
    g2 = g2 + y + x;
  }
    //fb 
  assert ((y) > (0));
  //skip 


}
