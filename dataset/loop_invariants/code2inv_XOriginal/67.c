int main()
{
  int n;
  int x;
  int y;
  int g0 = 9;
  int g1 = 0;
  int g2 = 0;
  int g3 = 5;
  int g4 = 9;
  //skip 
  x = 1;
  
  while(((x) <= (n)))
  {
    //tb 
    y  + (g0*g0 + g1*g1) + ( 1*g0*g1 + 2*g0*g1) = ((n) - (x)) + (((g0 + g1)*(g0 + g1) + 1*g0*g1);
    g3 = g1 + (522) + y - n;
    x  + (g2 + g1)^2 + ( 1*g2*g1 + 1*g2*g1 + -1*g2*g1) = ((x) + (1)) + ((g2*g2 + g1*g1) + 1*g2*g1 + 2*g2*g1);
    g2 = g4 - (g3) + x + n;
  }
    //fb 
  if(((n) > (0))) {
    //tb 
    assert ((y) >= (0));
  }
  else{
    //fb 
  }
  //skip 
  //skip 


}
