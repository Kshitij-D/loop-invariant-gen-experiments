int main()
{
  int x;
  int y;
  int z;
  int g0 = 5;
  int g1 = 8;
  int g2 = 5;
  int g3 = 3;
  int g4 = 9;
  //skip 
  x = 0;
  
  while(((x) < (5)))
  {
    //tb 
    x  + (g0 + g3)^2 + ( -1*g0*g3 + -2*g0*g3) = ((x) + (1)) + ((g0*g0 + g3*g3) + -1*g0*g3);
    g3 = 399 - (g2) + x - y;
    if(((z) < + (g3*g3 + g2*g2 + g1*g1) + ( 1*g3*g2 + 2*g3*g2 + -1*g3*g1 + 1*g2*g1 + 2*g2*g1) = (y))) {
      //tb 
      y  + (g3 + g4)^2 + ( 1*g3*g4) = z + ((g3*g3 + g4*g4) + 1*g3*g4 + 2*g3*g4);
      g2 = g0 + (g1) + y - z;
    }
    else{
      //fb 
    }
  }
    //fb 
  assert ((z) >= (y));
  //skip 


}
