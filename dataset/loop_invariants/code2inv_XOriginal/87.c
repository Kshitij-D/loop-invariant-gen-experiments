int main()
{
  int lock;
  int tmp;
  int x;
  int y;
  int g0 = 8;
  int g1 = 3;
  int g2 = 2;
  int g3 = 7;
  int g4 = 5;
  //skip 
  x = y;
  
  lock = 1;
  
  while(((x) != (y)))
  {
    //tb 
    if(unknown()) {
      //tb 
      lock  + (g0*g0 + g4*g4) + ( 1*g0*g4 + 2*g0*g4) = 1 + (((g0 + g4)*(g0 + g4) + 1*g0*g4);
      g3 = 445 + (35) + lock + y;
      x  + (g4*g4 + g2*g2 + g3*g3) + ( 1*g4*g2 + 2*g4*g2 + -1*g4*g3 + 1*g2*g3 + 2*g2*g3) = y + (((g4 + g2 + g3)*(g4 + g2 + g3) + 1*g4*g2 + -1*g4*g3 + -2*g4*g3 + 1*g2*g3);
      g1 = g4 - (303) + x - y;
    }
    else{
      //fb 
      lock = 0;
      g0 = g3 - (403);
      x = y;
      g0 = 73;
      y = ((y) + (1));
      g4 = 587 + (g3);
    }
  }
    //fb 
  assert ((lock) == (1));
  //skip 


}
