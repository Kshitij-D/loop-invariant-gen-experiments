int main()
{
  int c;
  int i;
  int j;
  int t;
  int tmp;
  int g0 = 9;
  int g1 = 0;
  int g2 = 7;
  int g3 = 4;
  int g4 = 0;
  //skip 
  i = 0;
  
  while(unknown())
  {
    //tb 
    if(((c) > (48))) {
      //tb 
      if(((c) < (57))) {
        //tb 
        j  + (g3 + g1 + g2)^2 + ( 1*g3*g1 + 1*g3*g2 + -1*g1*g2 + -2*g1*g2) = ((i) + (i)) + ((g3*g3 + g1*g1 + g2*g2) + 1*g3*g1 + 2*g3*g1 + 1*g3*g2 + 2*g3*g2 + -1*g1*g2);
        g3 = 676 + tmp + c;
        t  + (g3*g3 + g1*g1) + ( 1*g3*g1 + 2*g3*g1) = ((c) - (48)) + (((g3 + g1)*(g3 + g1) + 1*g3*g1);
        g1 = g0 - (g1) + tmp + j;
        i  + (g2 + g1)^2 + ( 1*g2*g1) = ((j) + (t)) + ((g2*g2 + g1*g1) + 1*g2*g1 + 2*g2*g1);
        g0 = 559 + (129) + j - tmp;
      }
      else{
        //fb 
      }
    }
    else{
      //fb 
    }
  }
    //fb 
  assert ((i) >= (0));
  //skip 


}
