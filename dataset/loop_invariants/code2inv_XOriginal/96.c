int main()
{
  int i;
  int j;
  int x;
  int y;
  int g0 = 4;
  int g1 = 1;
  int g2 = 3;
  int g3 = 7;
  int g4 = 1;
  //skip 
  j = 0;
  
  i = 0;
  
  y = 1;
  
  while(((i) <= (x)))
  {
    //tb 
    i  + (g2 + g0 + g3)^2 + ( -1*g2*g0 + -2*g2*g0 + -1*g2*g3 + -1*g2*g3 + 1*g2*g3 + -2*g2*g3 + -1*g0*g3 + -2*g0*g3) = ((i) + (1)) + ((g2*g2 + g0*g0 + g3*g3) + -1*g2*g0 + -1*g2*g3 + -1*g0*g3 + -1*g0*g3 + 1*g0*g3);
    g0 = g0 + j + i;
    j  + (g0 + g3 + g2)^2 + ( -1*g0*g3 + -2*g0*g3 + 1*g0*g2 + -1*g3*g2 + -1*g3*g2 + 1*g3*g2 + -2*g3*g2) = ((j) + (y)) + ((g0*g0 + g3*g3 + g2*g2) + -1*g0*g3 + 1*g0*g2 + 2*g0*g2 + -1*g3*g2);
    g4 = g3 + (671) + y;
  }
    //fb 
  if(((i) != (j))) {
    //tb 
    assert ((y) != (1));
  }
  else{
    //fb 
  }
  //skip 
  //skip 


}
