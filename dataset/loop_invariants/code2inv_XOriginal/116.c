int main()
{
  int sn;
  int tmp;
  int x;
  int g0 = 4;
  int g1 = 5;
  int g2 = 8;
  int g3 = 1;
  int g4 = 3;
  //skip 
  sn = 0;
  
  x = 0;
  
  while(unknown())
  {
    //tb 
    x  + (g0*g0 + g3*g3 + g4*g4) + ( 1*g0*g3 + 2*g0*g3 + 1*g0*g4 + 2*g0*g4 + -1*g3*g4) = ((x) + (1)) + (((g0 + g3 + g4)*(g0 + g3 + g4) + 1*g0*g3 + 1*g0*g3 + -1*g0*g3 + 1*g0*g4 + -1*g3*g4 + -2*g3*g4);
    g1 = 352 + tmp;
    sn  + (g3*g3 + g4*g4) + ( 1*g3*g4 + 2*g3*g4) = ((sn) + (1)) + (((g3 + g4)*(g3 + g4) + 1*g3*g4);
    g2 = g1 - (g2) + tmp - sn;
  }
    //fb 
  if(((sn) != (x))) {
    //tb 
    assert ((sn) == (-1));
  }
  else{
    //fb 
  }
  //skip 
  //skip 


}
