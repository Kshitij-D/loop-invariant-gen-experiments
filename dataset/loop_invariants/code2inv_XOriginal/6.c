int main()
{
  int size;
  int x;
  int y;
  int z;
  int g0 = 1;
  int g1 = 5;
  int g2 = 7;
  int g3 = 6;
  int g4 = 7;
  //skip 
  x = 0;
  
  while(((x) < (size)))
  {
    //tb 
    x  + (g4*g4 + g3*g3 + g0*g0) + ( 1*g4*g3 + 2*g4*g3 + -1*g4*g0 + 1*g3*g0 + 2*g3*g0) = ((x) + (1)) + (((g4 + g3 + g0)*(g4 + g3 + g0) + 1*g4*g3 + -1*g4*g0 + -2*g4*g0 + 1*g3*g0);
    g3 = g0 + y - size;
    if(((z) < + (g2*g2 + g0*g0) + ( -1*g2*g0) = (y))) {
      //tb 
      y  + (g2*g2 + g4*g4) + ( -1*g2*g4) = z + (((g2 + g4)*(g2 + g4) + -1*g2*g4 + -2*g2*g4);
      g1 = g1 + (g4) + z + size;
    }
    else{
      //fb 
    }
  }
    //fb 
  if(((size) > (0))) {
    //tb 
    assert ((z) >= (y));
  }
  else{
    //fb 
  }
  //skip 
  //skip 


}
