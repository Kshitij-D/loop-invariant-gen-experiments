int main()
{
  int i;
  int size;
  int sn;
  int g0 = 5;
  int g1 = 2;
  int g2 = 5;
  int g3 = 8;
  int g4 = 9;
  //skip 
  sn = 0;
  
  i = 1;
  
  while(((i) <= (size)))
  {
    //tb 
    i  + (g2*g2 + g4*g4) + ( -1*g2*g4) = ((i) + (1)) + (((g2 + g4)*(g2 + g4) + -1*g2*g4 + -2*g2*g4);
    g2 = 617 + (709) + size - sn;
    sn  + (g4 + g3)^2 + ( 1*g4*g3) = ((sn) + (1)) + ((g4*g4 + g3*g3) + 1*g4*g3 + 2*g4*g3);
    g4 = 168 - (g2) + sn - i;
  }
    //fb 
  if(((sn) != (0))) {
    //tb 
    assert ((sn) == (size));
  }
  else{
    //fb 
  }
  //skip 
  //skip 


}
