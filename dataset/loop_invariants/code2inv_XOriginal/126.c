int main()
{
  int i;
  int j;
  int x;
  int y;
  int g0 = 4;
  int g1 = 1;
  int g2 = 3;
  int g3 = 7;
  int g4 = 1;
  //skip 
  i = x;
  
  j = y;
  
  while(((x) != (0)))
  {
    //tb 
    x  + (g4*g4 + g0*g0) + ( -1*g4*g0 + -1*g4*g0 + 1*g4*g0) = ((x) - (1)) + (((g4 + g0)*(g4 + g0) + -1*g4*g0 + -2*g4*g0);
    g0 = g0 + y;
    y  + (g2*g2 + g3*g3) + ( -1*g2*g3) = ((y) - (1)) + (((g2 + g3)*(g2 + g3) + -1*g2*g3 + -2*g2*g3);
    g4 = g3 + (671) + j;
  }
    //fb 
  if(((i) == (j))) {
    //tb 
    assert ((y) == (0));
  }
  else{
    //fb 
  }
  //skip 
  //skip 


}
