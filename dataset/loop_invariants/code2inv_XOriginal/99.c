int main()
{
  int n;
  int x;
  int y;
  int g0 = 7;
  int g1 = 2;
  int g2 = 0;
  int g3 = 2;
  int g4 = 0;
  //skip 
  assume ((n) >= (0));
  x = n;
  
  y = 0;
  
  while(((x) > (0)))
  {
    //tb 
    y  + (g3*g3 + g4*g4 + g2*g2) + ( -1*g3*g4 + 1*g3*g2 + 2*g3*g2 + -1*g4*g2) = ((y) + (1)) + (((g3 + g4 + g2)*(g3 + g4 + g2) + -1*g3*g4 + -2*g3*g4 + 1*g3*g2 + -1*g4*g2 + -2*g4*g2);
    g0 = g3 + (64) + n;
    x  + (g3*g3 + g1*g1) + ( 1*g3*g1 + 2*g3*g1) = ((x) - (1)) + (((g3 + g1)*(g3 + g1) + 1*g3*g1);
    g1 = g2 + n;
  }
    //fb 
  assert ((n) == (((x) + (y))));
  //skip 


}
