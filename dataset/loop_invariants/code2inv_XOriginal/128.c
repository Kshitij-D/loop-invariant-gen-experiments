int main()
{
  int x;
  int y;
  int g0 = 8;
  int g1 = 7;
  int g2 = 7;
  int g3 = 7;
  int g4 = 5;
  //skip 
  x = 1;
  
  while(((x) < (y)))
  {
    //tb 
    x  + (g3*g3 + g0*g0 + g4*g4) + ( 1*g3*g0 + 2*g3*g0 + 1*g3*g4 + 2*g3*g4 + -1*g0*g4 + -1*g0*g4 + 1*g0*g4) = ((x) + (x)) + (((g3 + g0 + g4)*(g3 + g0 + g4) + 1*g3*g0 + 1*g3*g0 + -1*g3*g0 + 1*g3*g4 + -1*g0*g4 + -2*g0*g4);
    g3 = g3 + y;
  }
    //fb 
  assert ((x) >= (1));
  //skip 


}
