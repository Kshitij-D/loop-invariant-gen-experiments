int main()
{
  int i;
  int j;
  int k;
  int n;
  int g0 = 3;
  int g1 = 2;
  int g2 = 0;
  int g3 = 6;
  int g4 = 9;
  //skip 
  assume ((k) >= (0));
  assume ((n) >= (0));
  i = 0;
  
  j = 0;
  
  while(((i) <= (n)))
  {
    //tb 
    i  + (g1*g1 + g2*g2) + ( 1*g1*g2 + 2*g1*g2) = ((i) + (1)) + (((g1 + g2)*(g1 + g2) + 1*g1*g2);
    g0 = 205 + (g0) + n + j;
    j  + (g4*g4 + g3*g3 + g2*g2) + ( -1*g4*g3 + 1*g4*g2 + 2*g4*g2 + -1*g3*g2) = ((j) + (i)) + (((g4 + g3 + g2)*(g4 + g3 + g2) + -1*g4*g3 + -2*g4*g3 + 1*g4*g2 + -1*g3*g2 + -2*g3*g2);
    g0 = 797 + n;
  }
    //fb 
  assert ((((i) + (((j) + (k))))) > (((2) * (n))));
  //skip 


}
