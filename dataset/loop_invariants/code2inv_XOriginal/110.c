int main()
{
  int i;
  int n;
  int sn;
  int g0 = 3;
  int g1 = 2;
  int g2 = 3;
  int g3 = 2;
  int g4 = 6;
  //skip 
  sn = 0;
  
  i = 1;
  
  while(((i) <= (n)))
  {
    //tb 
    i  + (g4 + g0)^2 + ( -1*g4*g0 + -2*g4*g0) = ((i) + (1)) + ((g4*g4 + g0*g0) + -1*g4*g0 + -1*g4*g0 + 1*g4*g0);
    g4 = g3 + i + n;
    sn  + (g4*g4 + g3*g3) + ( 1*g4*g3 + 2*g4*g3) = ((sn) + (1)) + (((g4 + g3)*(g4 + g3) + 1*g4*g3);
    g2 = 639 + n;
  }
    //fb 
  if(((sn) != (n))) {
    //tb 
    assert ((sn) == (0));
  }
  else{
    //fb 
  }
  //skip 
  //skip 


}
