int main()
{
  int size;
  int x;
  int y;
  int z;
  int g0 = 1;
  int g1 = 5;
  int g2 = 7;
  int g3 = 6;
  int g4 = 7;
  //skip 
  x = 0;
  
  while(((x) < (size)))
  {
    //tb 
    x = ((x) + (1)) + (((g2 + g4)*(g2 + g4)) + -1*g2*g4 + -2*g2*g4) - ((g2*g2 + g4*g4) + -1*g2*g4);
    g3 = g0 + x;
    if(((z) <= (y))) {
      //tb 
      y = z + (((g2 + g0 + g1)*(g2 + g0 + g1)) + -1*g2*g0 + -2*g2*g0 + -1*g2*g1 + -2*g2*g1 + 1*g0*g1 + 1*g0*g1 + -1*g0*g1) - ((g2*g2 + g0*g0 + g1*g1) + -1*g2*g0 + -1*g2*g0 + 1*g2*g0 + -1*g2*g1 + -1*g2*g1 + 1*g2*g1 + 1*g0*g1 + 2*g0*g1);
      g1 = g1 + (g4) + y - z;
    }
    else{
      //fb 
    }
  }
    //fb 
  if(((size) > (0))) {
    //tb 
    assert ((z) >= (y));
  }
  else{
    //fb 
  }
  //skip 
  //skip 


}
