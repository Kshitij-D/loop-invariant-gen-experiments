int main()
{
  int size;
  int x;
  int y;
  int z;
  int g0 = 1;
  int g1 = 5;
  int g2 = 7;
  int g3 = 6;
  int g4 = 7;
  //skip 
  x = 0;
  
  while(((x) < (size)))
  {
    //tb 
    x  + (g4*g4 + g0*g0 + g2*g2) + ( 1*g4*g0 + 2*g4*g0 + 1*g4*g2 + 2*g4*g2 + 1*g0*g2 + 2*g0*g2) = ((x) + (1)) + (((g4 + g0 + g2)*(g4 + g0 + g2) + 1*g4*g0 + 1*g4*g2 + 1*g0*g2);
    g3 = g0 + x - size;
    if(((z) < + (g2 + g4)^2 + ( -1*g2*g4 + -2*g2*g4) = (y))) {
      //tb 
      y  + (g0*g0 + g1*g1) + ( 1*g0*g1 + 2*g0*g1) = z + (((g0 + g1)*(g0 + g1) + 1*g0*g1);
      g1 = g1 + (g4) + z + y;
    }
    else{
      //fb 
    }
  }
    //fb 
  if(((size) > (0))) {
    //tb 
    assert ((z) >= (y));
  }
  else{
    //fb 
  }
  //skip 
  //skip 


}
