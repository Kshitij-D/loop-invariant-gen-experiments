int main()
{
  int n;
  int x;
  int y;
  int g0 = 2;
  int g1 = 1;
  int g2 = 2;
  int g3 = 7;
  int g4 = 8;
  //skip 
  x = 1;
  
  while(((x) <= (n)))
  {
    //tb 
    y  + (g3*g3 + g1*g1) + ( -1*g3*g1) = ((n) - (x)) + (((g3 + g1)*(g3 + g1) + -1*g3*g1 + -2*g3*g1);
    g0 = g4 + (292) + y - n;
    x  + (g1 + g2)^2 + ( -1*g1*g2 + -2*g1*g2) = ((x) + (1)) + ((g1*g1 + g2*g2) + -1*g1*g2);
    g2 = g3 + x;
  }
    //fb 
  if(((n) > (0))) {
    //tb 
    assert ((y) < (n));
  }
  else{
    //fb 
  }
  //skip 
  //skip 


}
