int main()
{
  int i;
  int j;
  int x;
  int y;
  int g0 = 3;
  int g1 = 7;
  int g2 = 3;
  int g3 = 6;
  int g4 = 8;
  //skip 
  j = 0;
  
  i = 0;
  
  y = 2;
  
  while(((i) <= (x)))
  {
    //tb 
    i  + (g3*g3 + g4*g4) + ( 1*g3*g4 + 1*g3*g4 + -1*g3*g4 + 2*g3*g4) = ((i) + (1)) + (((g3 + g4)*(g3 + g4) + 1*g3*g4);
    g4 = 267 - (293) + i + y;
    j  + (g2*g2 + g4*g4 + g3*g3) + ( -1*g2*g4 + 1*g2*g3 + 2*g2*g3 + -1*g4*g3) = ((j) + (y)) + (((g2 + g4 + g3)*(g2 + g4 + g3) + -1*g2*g4 + -2*g2*g4 + 1*g2*g3 + -1*g4*g3 + -2*g4*g3);
    g0 = 444 - (782) + j;
  }
    //fb 
  if(((y) == (1))) {
    //tb 
    assert ((i) == (j));
  }
  else{
    //fb 
  }
  //skip 
  //skip 


}
