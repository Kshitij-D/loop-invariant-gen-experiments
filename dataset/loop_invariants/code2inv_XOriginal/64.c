int main()
{
  int x;
  int y;
  int g0 = 3;
  int g1 = 9;
  int g2 = 8;
  int g3 = 9;
  int g4 = 2;
  //skip 
  x = 1;
  
  while(((x) <= (10)))
  {
    //tb 
    y = ((10) - (x)) + ((g3*g3 + g4*g4 + g2*g2) + -1*g3*g4 + -1*g3*g2 + 1*g4*g2 + 2*g4*g2) - (((g3 + g4 + g2)*(g3 + g4 + g2)) + -1*g3*g4 + -2*g3*g4 + -1*g3*g2 + -2*g3*g2 + 1*g4*g2);
    g4 = 686 + (g3) + x + y;
    x = ((x) + (1)) + ((g4*g4 + g0*g0 + g2*g2) + 1*g4*g0 + 2*g4*g0 + 1*g4*g2 + 2*g4*g2 + 1*g0*g2 + 2*g0*g2) - (((g4 + g0 + g2)*(g4 + g0 + g2)) + 1*g4*g0 + 1*g4*g2 + 1*g0*g2);
    g2 = 829 - (561) + x - y;
  }
    //fb 
  assert ((y) < (10));
  //skip 


}
