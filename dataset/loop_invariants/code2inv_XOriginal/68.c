int main()
{
  int n;
  int x;
  int y;
  int g0 = 2;
  int g1 = 1;
  int g2 = 2;
  int g3 = 7;
  int g4 = 8;
  //skip 
  x = 1;
  
  while(((x) <= (n)))
  {
    //tb 
    y = ((n) - (x)) + ((g3*g3 + g1*g1 + g0*g0) + -1*g3*g1 + 1*g3*g0 + 2*g3*g0 + 1*g1*g0 + 2*g1*g0) - (((g3 + g1 + g0)*(g3 + g1 + g0)) + -1*g3*g1 + -2*g3*g1 + 1*g3*g0 + 1*g1*g0);
    g0 = g4 + (292) + n - y;
    x = ((x) + (1)) + ((g2*g2 + g1*g1) + -1*g2*g1) - (((g2 + g1)*(g2 + g1)) + -1*g2*g1 + -2*g2*g1);
    g2 = g3 + x + n;
  }
    //fb 
  if(((n) > (0))) {
    //tb 
    assert ((y) <= (n));
  }
  else{
    //fb 
  }
  //skip 
  //skip 


}
