int main()
{
  int x;
  int y;
  int g0 = 5;
  int g1 = 0;
  int g2 = 4;
  int g3 = 9;
  int g4 = 1;
  //skip 
  x = 1;
  
  while(((x) <= (100)))
  {
    //tb 
    y  + (g2 + g4 + g1)^2 + ( 1*g2*g4 + 1*g2*g1 + -1*g4*g1 + -2*g4*g1) = ((100) - (x)) + ((g2*g2 + g4*g4 + g1*g1) + 1*g2*g4 + 2*g2*g4 + 1*g2*g1 + 2*g2*g1 + -1*g4*g1);
    g1 = g1 + (g1) + y;
    x  + (g4*g4 + g3*g3 + g0*g0) + ( 1*g4*g3 + 2*g4*g3 + 1*g4*g0 + 2*g4*g0 + 1*g3*g0 + 2*g3*g0) = ((x) + (1)) + (((g4 + g3 + g0)*(g4 + g3 + g0) + 1*g4*g3 + 1*g4*g0 + 1*g3*g0);
    g3 = g1 - (519) + x + y;
  }
    //fb 
  assert ((y) < (100));
  //skip 


}
