int main()
{
  int lock;
  int tmp;
  int x;
  int y;
  int g0 = 9;
  int g1 = 3;
  int g2 = 6;
  int g3 = 6;
  int g4 = 9;
  //skip 
  y = ((x) + (1));
  
  lock = 0;
  
  while(((x) != (y)))
  {
    //tb 
    if(unknown()) {
      //tb 
      lock  + (g1 + g0)^2 + ( 1*g1*g0) = 1 + ((g1*g1 + g0*g0) + 1*g1*g0 + 2*g1*g0);
      g4 = g1 + (g0) + x + tmp;
      x  + (g1 + g3)^2 + ( -1*g1*g3 + -2*g1*g3) = y + ((g1*g1 + g3*g3) + -1*g1*g3);
      g1 = g2 - (g4) + lock + x;
    }
    else{
      //fb 
      lock = 0;
      g0 = 310 + (561);
      x = y;
      g0 = 184;
      y = ((y) + (1));
      g4 = g4 + (143);
    }
  }
    //fb 
  assert ((lock) == (1));
  //skip 


}
