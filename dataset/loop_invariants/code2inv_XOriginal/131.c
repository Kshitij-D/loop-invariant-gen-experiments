int main()
{
  int d1;
  int d2;
  int d3;
  int x1;
  int x2;
  int x3;
  int g0 = 4;
  int g1 = 8;
  int g2 = 5;
  int g3 = 9;
  int g4 = 7;
  //skip 
  d1 = 1;
  
  d2 = 1;
  
  d3 = 1;
  
  x1 = 1;
  
  while(((x1) > (0)))
  {
    //tb 
    if(((x2) > (0))) {
      //tb 
      if(((x3) > (0))) {
        //tb 
        x1  + (g1*g1 + g3*g3 + g0*g0) + ( -1*g1*g3 + 1*g1*g0 + 2*g1*g0 + -1*g3*g0) = ((x1) - (d1)) + (((g1 + g3 + g0)*(g1 + g3 + g0) + -1*g1*g3 + -2*g1*g3 + 1*g1*g0 + -1*g3*g0 + -2*g3*g0);
        g1 = g4 + (828) + x2 + x3;
        x2  + (g1*g1 + g2*g2) + ( 1*g1*g2 + 2*g1*g2) = ((x2) - (d2)) + (((g1 + g2)*(g1 + g2) + 1*g1*g2);
        g4 = g4 + (435) + d1;
        x3  + (g1 + g3)^2 + ( 1*g1*g3) = ((x3) - (d3)) + ((g1*g1 + g3*g3) + 1*g1*g3 + 2*g1*g3);
        g3 = g2 - (854) + d1 - x3;
      }
      else{
        //fb 
      }
    }
    else{
      //fb 
    }
  }
    //fb 
  assert ((x3) >= (0));
  //skip 


}
