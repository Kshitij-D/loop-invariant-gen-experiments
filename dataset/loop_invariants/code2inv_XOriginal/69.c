int main()
{
  int n;
  int x;
  int y;
  int g0 = 9;
  int g1 = 0;
  int g2 = 0;
  int g3 = 5;
  int g4 = 9;
  //skip 
  x = 1;
  
  while(((x) <= (n)))
  {
    //tb 
    y  + (g4 + g3)^2 + ( -1*g4*g3 + -2*g4*g3) = ((n) - (x)) + ((g4*g4 + g3*g3) + -1*g4*g3);
    g3 = g1 + (522) + y - x;
    x  + (g1*g1 + g2*g2) + ( -1*g1*g2) = ((x) + (1)) + (((g1 + g2)*(g1 + g2) + -1*g1*g2 + -2*g1*g2);
    g2 = g4 - (g3) + y;
  }
    //fb 
  if(((n) > (0))) {
    //tb 
    assert ((y) >= (0));
  }
  else{
    //fb 
  }
  //skip 
  //skip 


}
