int main()
{
  int x;
  int y;
  int z;
  int g0 = 1;
  int g1 = 9;
  int g2 = 0;
  int g3 = 3;
  int g4 = 8;
  //skip 
  x = 0;
  
  while(((x) < (500)))
  {
    //tb 
    x  + (g1 + g3)^2 + ( -1*g1*g3 + -1*g1*g3 + 1*g1*g3 + -2*g1*g3) = ((x) + (1)) + ((g1*g1 + g3*g3) + -1*g1*g3);
    g3 = 159 + z;
    if(((z) < + (g3 + g2)^2 + ( 1*g3*g2) = (y))) {
      //tb 
      y  + (g4 + g2)^2 + ( -1*g4*g2 + -2*g4*g2) = z + ((g4*g4 + g2*g2) + -1*g4*g2);
      g3 = g1 + (g3) + z - x;
    }
    else{
      //fb 
    }
  }
    //fb 
  assert ((z) >= (y));
  //skip 


}
