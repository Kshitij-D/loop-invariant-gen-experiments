int main()
{
  int x;
  int y;
  int g0 = 9;
  int g1 = 0;
  int g2 = 7;
  int g3 = 0;
  int g4 = 3;
  //skip 
  x = -15000;
  
  while(((x) < (0)))
  {
    //tb 
    x = ((x) + (y)) + ((g4*g4 + g2*g2) + 1*g4*g2 + 2*g4*g2) - (((g4 + g2)*(g4 + g2)) + 1*g4*g2);
    g1 = 286 + (g3) + y - x;
    y = ((y) + (1)) + ((g2*g2 + g1*g1 + g0*g0) + 1*g2*g1 + 2*g2*g1 + 1*g2*g0 + 2*g2*g0 + -1*g1*g0) - (((g2 + g1 + g0)*(g2 + g1 + g0)) + 1*g2*g1 + 1*g2*g0 + 1*g2*g0 + -1*g2*g0 + -1*g1*g0 + -2*g1*g0);
    g2 = g0 + (g0) + y;
  }
    //fb 
  assert ((y) > (0));
  //skip 


}
