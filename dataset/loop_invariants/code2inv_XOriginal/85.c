int main()
{
  int x;
  int y;
  int g0 = 9;
  int g1 = 0;
  int g2 = 7;
  int g3 = 0;
  int g4 = 3;
  //skip 
  x = -15000;
  
  while(((x) < (0)))
  {
    //tb 
    x  + (g2*g2 + g1*g1) + ( -1*g2*g1) = ((x) + (y)) + (((g2 + g1)*(g2 + g1) + -1*g2*g1 + -2*g2*g1);
    g1 = 286 + (g3) + y;
    y  + (g3 + g1 + g4)^2 + ( -1*g3*g1 + -2*g3*g1 + 1*g3*g4 + 1*g1*g4) = ((y) + (1)) + ((g3*g3 + g1*g1 + g4*g4) + -1*g3*g1 + -1*g3*g1 + 1*g3*g1 + 1*g3*g4 + 2*g3*g4 + 1*g1*g4 + 2*g1*g4);
    g2 = g0 + (g0) + x;
  }
    //fb 
  assert ((y) > (0));
  //skip 


}
