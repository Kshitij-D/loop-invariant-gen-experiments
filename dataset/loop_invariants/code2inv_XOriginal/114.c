int main()
{
  int sn;
  int tmp;
  int x;
  int g0 = 4;
  int g1 = 5;
  int g2 = 8;
  int g3 = 1;
  int g4 = 3;
  //skip 
  sn = 0;
  
  x = 0;
  
  while(unknown())
  {
    //tb 
    x  + (g2*g2 + g4*g4) + ( 1*g2*g4 + 2*g2*g4) = ((x) + (1)) + (((g2 + g4)*(g2 + g4) + 1*g2*g4);
    g1 = 352 + x + tmp;
    sn  + (g3 + g0)^2 + ( 1*g3*g0) = ((sn) + (1)) + ((g3*g3 + g0*g0) + 1*g3*g0 + 2*g3*g0);
    g2 = g1 - (g2) + x;
  }
    //fb 
  if(((sn) != (x))) {
    //tb 
    assert ((sn) == (-1));
  }
  else{
    //fb 
  }
  //skip 
  //skip 


}
