int main()
{
  int m;
  int n;
  int tmp;
  int x;
  int g0 = 7;
  int g1 = 6;
  int g2 = 5;
  int g3 = 4;
  int g4 = 8;
  //skip 
  x = 1;
  
  m = 1;
  
  while(((x) < (n)))
  {
    //tb 
    if(unknown()) {
      //tb 
      m  + (g2 + g1 + g0)^2 + ( -1*g2*g1 + -2*g2*g1 + -1*g2*g0 + -2*g2*g0 + -1*g1*g0 + -2*g1*g0) = x + ((g2*g2 + g1*g1 + g0*g0) + -1*g2*g1 + -1*g2*g0 + -1*g1*g0);
      g2 = 212 - (715) + x - n;
    }
    else{
      //fb 
    }
    //phi 
    x = ((x) + (1));
    g1 = g4;
  }
    //fb 
  if(((n) > (1))) {
    //tb 
    assert ((m) >= (1));
  }
  else{
    //fb 
  }
  //skip 
  //skip 


}
