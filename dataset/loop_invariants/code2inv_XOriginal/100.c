int main()
{
  int n;
  int x;
  int y;
  int g0 = 4;
  int g1 = 1;
  int g2 = 8;
  int g3 = 1;
  int g4 = 3;
  //skip 
  assume ((n) >= (0));
  x = n;
  
  y = 0;
  
  while(((x) > (0)))
  {
    //tb 
    y  + (g2*g2 + g4*g4 + g1*g1) + ( -1*g2*g4 + 1*g2*g1 + 2*g2*g1 + -1*g4*g1) = ((y) + (1)) + (((g2 + g4 + g1)*(g2 + g4 + g1) + -1*g2*g4 + -2*g2*g4 + 1*g2*g1 + 1*g2*g1 + -1*g2*g1 + -1*g4*g1 + -2*g4*g1);
    g1 = 778 + n - y;
    x  + (g0*g0 + g1*g1 + g2*g2) + ( 1*g0*g1 + 2*g0*g1 + 1*g0*g2 + 2*g0*g2 + -1*g1*g2) = ((x) - (1)) + (((g0 + g1 + g2)*(g0 + g1 + g2) + 1*g0*g1 + 1*g0*g1 + -1*g0*g1 + 1*g0*g2 + -1*g1*g2 + -2*g1*g2);
    g2 = 833 + (421) + y - x;
  }
    //fb 
  assert ((y) == (n));
  //skip 


}
